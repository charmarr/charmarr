juju:
  channel: 3.6/stable
  model-defaults:
    logging-config: <root>=WARNING; unit=DEBUG
    update-status-hook-interval: 60m

providers:
  microk8s:
    enable: true
    bootstrap: true
    channel: 1.32-strict/stable
    addons:
      - dns
      - hostpath-storage
      - metallb
